...` -> `# تطبيق FastAPI — /health، /upload، /chat، /profile، /employees، /n8n-workflows، /webhook/*`
            *   `# Messaging + voice channel adapters` -> `# محولات قنوات المراسلة + الصوت`
            *   `# Twilio + Gupshup` -> `# Twilio + Gupshup`
            *   `# Twilio Media Streams, WebSocket server` -> `# Twilio Media Streams، خادم WebSocket`
            *   `# documents + chunks tables, pgvector index` -> `# جداول المستندات + الأجزاء، فهرس pgvector`
            *   `# Runnable example scripts` -> `# برامج نصية برمجية قابلة للتشغيل كأمثلة`
            *   `# CI: compile check, Docker build, smoke tests` -> `# التطوير المستمر (CI): فحص الترجمة، بناء Docker، اختبارات الدخان`
            *   `# app + db + voice services` -> `# خدمات التطبيق + قاعدة البيانات + الصوت`

    *   **Section: What's in the hosted version (ragleap.com)**
        *   `## What's in the hosted version (ragleap.com)`
            -> `## ماذا يوجد في النسخة المستضافة (ragleap.com)`
        *   `RagLeap Core covers document upload, retrieval, and web chat. The hosted platform builds a full AI business manager on top of it:`
            -> `يغطي Rag
