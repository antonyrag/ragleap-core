# Twilio + Gupshup`
            *   `# Twilio Media Streams, WebSocket server` -> `# Twilio Media Streams, WebSocket-bediener`
            *   `# documents + chunks tables, pgvector index` -> `# dokumente + brokke-tabelle, pgvector-indeks`
            *   `# Runnable example scripts` -> `# Uitvoerbare voorbeeldskrifte`
            *   `# CI: compile check, Docker build, smoke tests` -> `# CI: samestellingskontrole, Docker-bou, rooktoetse`
            *   `# app + db + voice services` -> `# toep- + db- + stemsdienste`

    *   *Section: What's in the hosted version (ragleap.com)*:
        *   `## What's in the hosted version (ragleap.com)` -> `## Wat is in die gehoste weergawe (ragleap.com)`
        *   `RagLeap Core covers document upload, retrieval, and web chat. The hosted platform builds a full AI business manager on top of it:` -> `RagLeap Core dek dokument-oplaai, herwinning en webklets. Die gehoste platform bou 'n volledige KI-besigheidsbestuurder bo-op dit:`
        *   Table headers: `| Area | What it adds |` -> `| Gebied | Wat dit byvoeg |`
        *   Table rows:
            *   `| **
