# Адаптери за съобщения + гласови канали
        *   `# Twilio + Gupshup` -> `# Twilio + Gupshup`
        *   `# Twilio Media Streams, WebSocket server` -> `# Twilio Media Streams, WebSocket сървър`
        *   `# documents + chunks tables, pgvector index` -> `# таблици за документи + части (chunks), pgvector индекс`
        *   `# Runnable example scripts` -> `# Изпълними примерни скриптове`
        *   `# CI: compile check, Docker build, smoke tests` -> `# CI: проверка на компилацията, Docker компилация, бързи тестове (smoke tests)`
        *   `# app + db + voice services` -> `# услуги за приложение + база данни + глас`

    *   `## What's in the hosted version (ragleap.com)` -> `## Какво има в хостваната версия (ragleap.com)`
    *   `RagLeap Core covers document upload, retrieval, and web chat. The hosted platform builds a full AI business manager on top of it:`
        -> `RagLeap Core покрива качването на документи, извличането и уеб чата. Хостваната платформа изгражда пълен AI бизнес мениджър върху него:`
    *   Table translation:
        *   `Area` -> `Област`
        *   `What it adds`
