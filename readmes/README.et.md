koosinussarnasusotsing`
        *   `generation.py      # 19-provider BYOK generation (Gemini, OpenAI, Anthropic, etc.)` -> `generation.py      # 19 teenusepakkujaga BYOK genereerimine (Gemini, OpenAI, Anthropic jne)`
        *   `ingest.py          # chunk -> embed -> store pipeline` -> `ingest.py          # tükelda -> pesasta -> salvesta konveier`
        *   `parsers.py         # PDF/DOCX/TXT text extraction` -> `parsers.py         # PDF/DOCX/TXT teksti eraldamine`
        *   `employees/         # AI Employees — roles, business profile, learned memory` -> `employees/         # AI-töötajad — rollid, äriprofiil, õpitud mälu`
        *   `workflows.py       # n8n workflow automation — webhook triggers` -> `workflows.py       # n8n töövoo automatiseerimine — veebikonksu käivitajad`
        *   `api.py             # FastAPI app — /health, /upload, /chat, /profile, /employees, /n8n-workflows, /webhook/*` -> `api.py             # FastAPI rakendus — /health, /upload, /chat, /profile, /employees, /n8n-workflows, /webhook/*`
        *
