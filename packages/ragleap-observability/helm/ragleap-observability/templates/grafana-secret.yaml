{{- if .Values.grafana.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ragleap-grafana-admin
type: Opaque
stringData:
  admin-user: admin
  admin-password: change-me-before-real-use   # REPLACE via --set or a real secrets workflow before production use
{{- end }}
